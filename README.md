#ucontinue-api


##Routes
------

**GET /stories**

Returns top 10 stories in decending order.

**GET /stories/:id**

Gets a story by id.

**POST /stories/:id/continuations**

Add a story continuation.